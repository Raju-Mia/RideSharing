from django.urls import path
# from .views import VehicleHistory, ModelHistory, HistoryDetails, HistoryOfSpecificObject


app_name = "log"


urlpatterns = [
    # path("v1/history-of-specific-object/<str:model>/<uuid:object_id>/", HistoryOfSpecificObject.as_view(), name="history_of_specific_object"),
    # path("v1/model-history/<str:model>/", ModelHistory.as_view(), name="model_history"),
    # path("v1/history-details/<str:model>/<int:history_id>/", HistoryDetails.as_view(), name="history_details"),

]
